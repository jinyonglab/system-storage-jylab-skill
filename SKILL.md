---
name: system-storage-jylab
description: >
  macOS / Windows 专业只读存储治理技能（自动识别系统）。用于磁盘满了、
  C盘/硬盘空间不足、storage analysis、disk cleanup、清缓存、清理磁盘、查找占空间
  大户、生成可执行治理报告等场景。扫描阶段严格只读，输出交互式 HTML 工作台；工作台将
  条目分为立即处置候选 / 人工复核候选 / 通过正规流程处理三类，并突出执行边界、风险因素、
  回滚方式、释放空间估算和处置路径。若用户明确询问 RAM/运行内存/进程内存占用，不属于本 skill。
---

# System Storage JYLab

面向 macOS / Windows 的专业存储治理流程。目标不是“扫到什么删什么”，而是以系统管理员和架构师视角，给出可审计、可回滚、可执行的处置工作台。

## 铁律

- **扫描全程只读。** 只能跑扫描、统计、列目录、读元信息（如 df、du、diskutil、stat、ls、os.scandir）。不得在扫描阶段执行 rm、mv、rmdir、清空回收站、改权限、卸载软件等写操作。
- **删除必须有二次确认。** 报告里展示的命令供用户自行确认；服务模式按钮也必须走白名单、token、Host 校验、浏览器 confirm。用户即使说“帮我删”，也先确认具体路径、分级和风险。
- **风险先行。** 清理建议必须说明数据属性、影响面、回滚方式、需要关闭的进程、释放空间是否需要清空废纸篓/回收站。
- **估算标注清楚。** 涉及“可释放空间”一律说明是估算值；不要把 APFS 可清除空间、压缩文件、硬链接、云盘占位文件当作绝对可释放空间。
- **路径、命令保留原文不翻译。**
- **不碰系统关键路径。** 系统目录、组件存储、虚拟内存、休眠文件、APFS 快照、应用核心数据、数据库文件默认不手删，只给正规入口或系统工具。

## 安全边界

- **先判断，再操作。** 绿灯表示“可以先清”；黄灯表示“必须先看”；红灯表示“不要在工作台里直接删，只能走正规卸载或应用内流程”。
- **按钮权限分层。** `移到废纸篓/回收站` 优先于 `直接删除`；黄灯最多给“打开文件夹”与“仅安全部分移到废纸篓”；红灯不给破坏性按钮。
- **路径必须可解释。** 只有当 agent 能说明“这是什么、为什么能动、删后会怎样”时，才允许给 `trash_paths`。
- **遇到混合目录默认保守。** 同一目录内同时有缓存、数据库、聊天附件、离线资源、程序本体时，不给整目录自动清理能力。
- **遇到这些情况必须停下确认。** 用户要求直接删黄灯/红灯目录；路径超出用户主目录白名单；目录属于同步盘、企业应用、数据库、虚拟机、容器 volume、系统目录。
- **交互提示必须说人话。** 点击前说明会发生什么、能否恢复、什么时候真正释放空间、处理后要验证什么，避免只给按钮不给后果说明。

## 架构

- `scripts/scan.py`：零依赖只读扫描器，自动识别 macOS / Windows，输出 `/tmp/storage_scan.json`。
- `references/macos.md`、`references/windows.md`：平台目录语义、分级规则、风险目录和清理入口。
- `references/reporting.md`：analysis JSON 写作规范，约束证据、置信度、影响面、回滚与验证描述。
- `scripts/validate_analysis.py`：报告生成前的质量闸门，校验关键字段、路径边界和分级契约。
- `scripts/build_report.py`：把分析 JSON 注入 `assets/report_template.html`，生成静态 HTML 报告。
- `scripts/server.py`：本地 127.0.0.1 服务，提供受保护的打开文件夹、移入废纸篓/回收站、绿灯直接删除接口。
- `assets/report_template.html`：交互式工作台模板，渲染磁盘概览、执行摘要、证据化卡片、执行边界、处置分组和后续治理建议。

## 执行流程

### Step 1 只读扫描

```bash
python3 scripts/scan.py > /tmp/storage_scan.json
```

`scan.py` 自动识别系统（`sys.platform`）：
- **macOS**：扫 home、library、caches、containers、group_containers、app_support、applications、downloads、dev_caches，用 `du` 算大小。
- **Windows**：扫 user_profile、appdata_local、appdata_roaming、temp、downloads、program_files(_x86)、dev_caches，用 `os.scandir` 算大小；`system.disks` 含所有盘符。

输出 JSON：`system`（系统/磁盘信息，含 `disk_name` 主盘名 + `disks` 全部盘）+ `groups`（各组子目录大小，已降序、过滤 50MB 以下）。扫描较慢，耐心等。读不到的目录标 `denied`，需在报告里列出并提示遗漏体量。

### Step 2 分析与分级

先看 `system.os` 判断系统，读对应的数据布局参考：macOS 读 [references/macos.md](references/macos.md)，Windows 读 [references/windows.md](references/windows.md)（讲该系统东西存哪、怎么辨认、归哪一级）。生成最终 analysis JSON 前，再读 [references/reporting.md](references/reporting.md) 控制报告写法。然后读 `/tmp/storage_scan.json` 做这几件事：

1. **挑 Top 5** 占用大户，判定类型（系统资产/应用本体/应用数据/应用缓存/开发缓存/用户文件/媒体内容/下载内容/虚拟机镜像/回收站/其他）。
2. **识别“神秘大目录”。** UUID 命名的 Container、不明隐藏目录、大体量 AppData，要追查它属于哪个 App、装的是什么。必要时 `ls`/`du` 或只读枚举深入一层看清楚，但仍只读。
3. **三级分类 = 清理决策清单，不是全盘点。** 只把"存在'要不要动它'这个决策"的项放进三灯；日常在用的正常应用、操作系统本身、海量零碎小文件没有清理决策，不进三灯，它们落在磁盘条的蓝色"系统及其他"里。判定标准：
   - 🟢 **可自动清理**：纯缓存、临时文件、安装包残留、明确可再生且不影响功能、不丢用户数据（pip/uv/npm/Xcode DerivedData 等开发缓存、浏览器缓存）。
   - 🟡 **需人工判断**：含用户数据或有判断成本（离线视频、文档、项目代码 node_modules、聊天记录、设计稿、虚拟机镜像、Docker volume、云盘同步目录）。给内容画像 + 处置路径 + 风险提示。**所有橙灯项在服务模式下自动有「在访达/资源管理器打开」按钮**；只有核实过且删了不破坏 App 的安全子路径才能给 `trash_paths`，且橙灯只准移废纸篓/回收站，绝不给“直接删除”。App 托管又无安全子路径的（Chrome/微信/Teams/OneDrive 配置库）只给打开按钮、不给 `trash_paths`。如果某项在文件管理器里是 App 内部格式、不方便手动挑选，给 `open_note` 字段做客观说明。
   - 🔴 **谨慎清理（有决策但不建议手删）**：你可能想动、但建议别手删的具体项——重复安装的应用、想卸载的大应用、运行中应用的核心数据等。给"为什么不建议手删" + `indirect_release` 写**具体卸载步骤**（自带卸载器 / 启动台长按 / 右键移废纸篓 / AppCleaner 清残留 / App Store 可重装等，要可照做不是空话）。应用项给 `app_paths`（真实 `.app` 绝对路径数组）→ 网页出现「在文件管理器打开（去卸载）」按钮，定位到 App 让用户自己正规卸载。**红灯不给删除/卸载按钮**（应用在系统目录、可能要管理员密码、可能有自带卸载器和残留，后台代删不稳妥）。**纯系统文件、APFS 快照不要单独列红卡**（没有清理决策），归蓝色即可；系统层面的释放技巧（重启释 swap、Time Machine 快照策略、可清除空间自动回收）写进 `summary.long_term` 长期建议。

每个 🟢 项要给：预估释放空间、清理前需关闭的进程、可一键复制的清理命令（用移到废纸篓或 App 自身清理入口的安全方式，谨慎用 `rm`；如用 `rm` 必须是明确的缓存子目录）。

**大小字段写干净**：`size` / `size_estimate` 用"约 14 GB""合计约 8.6 GB"即可——"约"已表示估算，不要再加"（估算）"，重复且不专业（模板也会自动去掉这种冗余括号）。可再生属性已由分级标题和按钮说明覆盖，别塞进大小字段。

尽量补这些增强字段，让报告更像运维决策文档而不是清单：

- `confidence`：`high` / `medium` / `low`
- `impact_scope`：影响面
- `rollback`：回滚方式
- `verification`：清理后如何验证
- `recommended_window`：建议操作时机
- `evidence`：1-3 条证据
- 如需双语工作台，为关键文本补 `<field>_en` 英文镜像字段，供前端按钮切换中英文显示

### Step 3 专业风险评估

在生成 analysis JSON 前，必须形成可审计判断：

- **数据属性**：缓存、临时文件、用户资料、应用数据库、同步目录、虚拟机/容器镜像、系统资产。
- **影响面**：是否影响登录态、离线内容、聊天记录、工程依赖重装、构建速度、系统启动、企业管控软件。
- **可恢复性**：直接删除不可恢复；废纸篓/回收站可恢复但清空后不可恢复；应用内清理通常更安全；云盘删除可能同步到云端。
- **操作窗口**：清理浏览器、IDE、Docker、虚拟机、聊天软件、同步盘前先退出相关进程；企业电脑需避开工作中同步/备份窗口。
- **证据质量**：如果只是目录名推断，要写成“疑似”；如果已通过子目录、bundle id、文件类型核实，可写成“确认”。

### Step 4 校验 analysis JSON

生成 analysis JSON 后先做一次校验：

```bash
python3 scripts/validate_analysis.py /tmp/storage_analysis.json
```

目标：

- 绿灯 `trash_paths` 不为空
- 关键摘要字段类型正确
- 可解析容量字段正常
- 删除白名单路径不越界
- 黄灯 / 红灯不出现越权删除路径
- 每个可操作项都能说明回滚方式与清理后验证

若校验失败，先修 analysis JSON，再进入 HTML 生成。

### Step 5 生成交互报告

把分析结果写成 analysis JSON（schema 见 `scripts/build_report.py` 顶部注释）。

**🟢 项必须带 `trash_paths`**（具体可删的绝对路径数组，区别于人类可读的 `path` 展示字段）——这是网页删除按钮的前提，漏了按钮就不出现。

**默认用一键删除模式（`server.py`）打开报告**，因为这个 skill 的核心价值就是网页上能直接清理：
```bash
python3 scripts/server.py /tmp/storage_analysis.json   # 自动开浏览器，Ctrl+C 停
```
`server.py` 起在 127.0.0.1 + 随机端口 + 随机 token。🟢 项给「移到废纸篓」(可逆) +「直接删除」(立即释放、不可逆)；🟡 项给「在访达打开」+（有安全子路径时）「移到废纸篓」。**安全模型——三套白名单，权限从严到宽**：`rm` 只允许绿灯 `trash_paths`；`trash` 允许绿灯+橙灯 `trash_paths`（橙灯永远不能 rm）；`open`（在文件管理器打开，非破坏性）允许上述全部 + 橙灯真实 `path`。所有请求 realpath 校验 + 必须在 $HOME 内 + token + Host 校验，每次点击浏览器先 confirm。osascript/SHFileOperationW 入废纸篓，macOS 首次弹访达自动化授权点允许即可。

交互要求：

- 红灯分组在报告中必须使用更强的红色警示表达，至少在容量概览和分组标题上显式突出“谨慎清理”。
- 所有破坏性动作文案都要先写恢复条件，再写释放空间效果。
- 如果当前报告是静态 HTML，要明确提示“这里只能看，不能删”；如果是服务模式，要明确提示“哪些按钮可逆，哪些不可逆”。

仅当用户明确只想要一份可分享/留存的只读文件时，才用静态模式（无删除按钮，因为 `file://` 打开的页面碰不到文件系统）：
```bash
python3 scripts/build_report.py /tmp/storage_analysis.json ~/Desktop/storage-report.html && open ~/Desktop/storage-report.html
```

**排障：网页上没有删除/移废纸篓按钮** = 要么开的是静态报告（改用 `server.py`），要么 🟢 项漏了 `trash_paths`（补上重启服务）。

报告阅读流（固定顺序）：磁盘总览卡片（容量 + 进度条 + 三色容量 pills + 系统信息，纯数据）→ 占用排行 Top5 → 执行建议 → 🟢🟡🔴 三级可折叠卡片（命令一键复制）→ 长期优化建议。即"现状 → 诊断 → 处方 → 操作 → 预防"。

注意 `summary.overview` 要写成一句话洞察（直接说最大占用是什么、能释放多少），不要重复总/已用/可用数字——那些已在卡片大数字里显示。overview 渲染在"执行建议"小节开头作引子（普通文字），紧接着是 `summary.priority` 优先级清单。

报告必须补齐两个专业块：

- `summary.cleanup_notes`：清理前注意事项，写 4-7 条。覆盖备份、关闭应用、废纸篓/回收站、云盘同步、管理员权限、二次扫描验证。
- `summary.risk_factors`：风险因素，写 4-7 条。覆盖误删用户数据、App 数据库损坏、同步盘连带删除、直接删除不可恢复、系统目录误操作、Windows/macOS 平台差异。

磁盘进度条把"已用"拆成分段：绿(可自动清)+橙(需手动)+红(已识别的不建议动项)+蓝(系统及其他，自动取 已用−绿−橙−红 的余量)，余下为可用(灰底)。`summary.tier_stats` 的 green / yellow / red 三个值都要以可解析的 GB 数字开头（如 "约 27.8 GB"），脚本从中取数算分段；蓝色段和"系统及其他"pill 由模板自动算余量。

pills 只渲染解析出的纯数字（如"约 5.5 GB"），不显示数据里的附注，所以 tier_stats 三个值写干净的数字即可，别加"仅已识别项/系统未计"这类道歉式说明——系统文件本来就归在蓝色段，红色只放你能量化的 🔴 项（重复应用、可卸载大应用等），量不准的系统文件/快照自然落到蓝色。

### Step 6 对话里给摘要

报告生成后，在对话里用一段话给结论先行的摘要：总可释放估算、最该先清的 2-3 项、风险最高的一项、建议优先采用“移到废纸篓/回收站 + 复扫验证”的路径。细节让用户看网页。

## 依赖与运行前提

- 全部脚本是 **Python 3 标准库**，零第三方依赖（不用 pip install）。
- **macOS** 自带 python3、`du`、`diskutil`、`osascript`，开箱即用。
- **Windows** 默认没装 Python——需先装 Python 3，且命令多为 `python` 或 `py -3`（不是 `python3`）。本 skill 命令示例写的是 `python3`，在 Windows 上自动改用 `python` / `py -3`。
- 本 skill 是 **agent 驱动**：扫描出数据后由 agent 做分级分析，不是双击即用的独立 App。

## 发布与脱敏约束

- 若我要将本 skill 作为公开 GitHub 仓库发布，**我不会提交任何真实扫描结果、analysis JSON、最终 HTML 报告、截图或日志**。
- 我知道 `scan.py` 输出天然可能含用户名、主目录、磁盘卷标、软件目录和项目路径；这些都属于环境信息，所以我会在公开前完成脱敏或直接排除。
- `.pyc`、`__pycache__`、临时日志和本地调试产物不属于 skill 交付内容，我不会把它们保留在仓库里。
- 如果我需要提供演示材料，我只会使用人工脱敏后的样例数据，并将路径、用户名、磁盘名、同步盘名、聊天软件痕迹、客户文件名改成通用占位。

## 平台状态

- **macOS**：完整实现并实测（扫描 / 报告 / 一键删除全验证过）。
- **Windows**：代码已写（`scan.py` 的 `scan_windows`、`server.py` 的 `_trash_windows` 走 `SHFileOperationW`），但**未在真实 Windows 上实测**。首次在 Windows 跑要核对：目标目录路径、`os.scandir` 大小、回收站删除是否正常。多盘符已支持（主盘分段条 + 其他盘列表）。

## 报告质量标准

- **专业性**：结论先行、证据充分、风险明确、建议可执行，避免“应该没事”“看起来可以删”。
- **可操作性**：每个清理项都要有路径、大小、分类、操作步骤、风险、回滚方式或替代入口。
- **保守性**：用户资料、数据库、同步盘、虚拟机、容器 volume、系统目录默认不自动删。
- **复核性**：报告能解释“为什么放在这个颜色”，并能让用户按路径复查。
- **可发布性**：保持零第三方依赖、脚本注释清楚、README 能说明架构、能力边界、运行方式和安全模型。

## 长期优化建议素材（写进 `summary.long_term`）

- 定期清理：`brew cleanup`、Xcode DerivedData、浏览器缓存
- 可视化工具：DaisyDisk、GrandPerspective、OmniDiskSweeper
- 大文件归档到外置盘 / iCloud / NAS；macOS「系统设置 > 通用 > 储存空间」的优化选项
- Windows 存储感知、磁盘清理、DISM 组件清理；把大型游戏库/素材库迁移到非系统盘
- Docker / 虚拟机 / IDE 缓存设定定期维护窗口，避免在构建、备份、同步中清理
